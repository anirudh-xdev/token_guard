---
description: TokenGuard product mental model and hard invariants for every session
alwaysApply: true
---

# TokenGuard overview

TokenGuard is a Go reverse proxy / financial firewall for LLM APIs.

- Guarded mode: Turso budgets + usage, Upstash Redis loop breaker, `pricing.json` costs
- Proxy-only: `TOKENGUARD_GUARD_ENABLED=false` (no budget/loop)
- Mgmt/dashboard require guard enabled and `TOKENGUARD_ADMIN_SECRET` (16+ chars)

## Surfaces

| Audience | Path | Auth |
|----------|------|------|
| End users | Next.js `web/` `/portal` → Go `/portal/api/*` | Clerk JWT |
| Operators | Next.js `web/` `/dashboard` → Go `/mgmt/*` | Admin secret |
| Integrators | `/v1/*` with `tg_` | `X-TokenGuard-API-Key` |

Do not mix portal users into operator mgmt APIs.

## Do not

- Enable mgmt without guard wiring
- Store plaintext API keys in the DB
- Soft-fail open on missing Redis/Turso in guarded mode
- Guess listen ports — check `TOKENGUARD_LISTEN_ADDR` / `.env` (default `:8080`)
